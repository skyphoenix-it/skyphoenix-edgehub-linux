# EdgeHub v1.0.1

Release version: `v1.0.1`
Release stage: stable

This checkout is unreleased. `v1.0.1` is the working target for 1.0 maintenance
on `master`; nothing here is published or certified, and no availability,
performance or stability claim is made for it.

The published `v1.0.0` notes remain on its
[GitHub release](https://github.com/skyphoenix-it/skyphoenix-edgehub-linux/releases/tag/v1.0.0),
and the per-version history is in `CHANGELOG.md`.

## Changes

Nothing recorded yet. Entries accumulate under `## [Unreleased]` in
`CHANGELOG.md` and are moved into a dated heading when this target is prepared
for release, at which point `release-metadata.toml` moves to `candidate`.
