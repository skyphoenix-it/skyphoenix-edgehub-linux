---
description: Supervised autonomous read-only session (framework-managed; inspection commands only).
mode: primary
permission:
  bash:
    '*': deny
    cat*: allow
    find*: allow
    git branch*: allow
    git diff*: allow
    git log*: allow
    git show*: allow
    git status*: allow
    grep*: allow
    head*: allow
    ls*: allow
    pwd: allow
    rg*: allow
    tail*: allow
    wc*: allow
  edit: deny
  external_directory: deny
  webfetch: deny
---

<!-- GENERATED by scripts/agent-framework/render.py — used by scripts/agent-framework/provider-opencode.sh via `opencode run --agent`. Explicit allow/deny only: unattended runs must never depend on interactive approvals, and must not reach outside the assigned worktree. -->

# af-supervised-readonly

Framework-managed agent profile for supervised autonomous runs. The role brief for the active task is provided in the prompt; this profile only fixes the permission envelope.
